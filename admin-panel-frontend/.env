PORT=3003
HOST=0.0.0.0

# Optional: Enable debug mode
REACT_APP_DEBUG=true

# NOTE: REACT_APP_API_URL and REACT_APP_WS_URL are intentionally NOT set
# to allow the frontend to auto-detect the correct URLs based on the browser's
# hostname. This enables remote access via Tailscale without hardcoding localhost.
